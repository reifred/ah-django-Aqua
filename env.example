"""

This file contains sample environment variables used across the project.

Its main purpose is to track the new variables that have been added to the project.

Incase you add any environment variable in the project endevour to add the sample
in this file so that it enables other contributors to know which environment
variables to set in there environments.

NOTE:
- Always set these variables in your environments before testing the project
- Follow the convention where keys are UPPERCASE i.e DATABASE_HOST, DATABASE_NAME e.t.c
- The values should just be samples where one will replace with their values 
i.e your_db_name, your_db_username e.t.c

"""
# PostgreSQL connection variables

export DATABASE_NAME='your_db_name'
export DATABASE_USER='your_db_username'
export DATABASE_PASSWORD='your_db_password'
export DATABASE_HOST='localhost'
export SECRET_KEY='app_secret_key'

# Email connection variables
export EMAIL_HOST_USER='your_email'
export EMAIL_HOST_PASSWORD='your_email_password'
export EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend'
export EMAIL_PORT=587
export EMAIL_HOST='smtp.gmail.com'
export EMAIL_USE_TLS=True

export HOME=''

# Social Authentication variables
export TWITTER_CONSUMER_KEY='your_twitter_consumer_key'
export TWITTER_CONSUMER_SECRET='your_twitter_consumer_secret'
